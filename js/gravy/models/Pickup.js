/**
 * A date and time when a Vendor will be at a location.
 *
 * Most interesting properties (Vendor & Location) are stored
 * on the asscociated PickupSchedule this Pickup was generated
 * from.
 */
define([
  'wrap!backbone'
], function(
  Backbone
) {
  return Backbone.Model.extend({
  });
});
